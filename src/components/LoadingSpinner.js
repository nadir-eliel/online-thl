import Spinner from 'react-bootstrap/Spinner';

export const LoadingSpinner = () => {
  return <Spinner animation="border" />;
}

/**TODO:
 * para este spinner hacer que el color de loading sea azul,
 * que se siga mostrando el 'OnlineContainer'
 * Hacer que sea un poquito mas grande  y que esté centrado
 */